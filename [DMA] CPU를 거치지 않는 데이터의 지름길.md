# [DMA] CPU를 거치지 않는 데이터의 지름길

> 작성자: 김시훈(Kim Sihoon)

-----

`DMA(Direct Memory Access)`는 주변장치가 CPU의 개입 없이 시스템 메모리에 직접 접근하여 데이터를 주고받을 수 있게 해주는 하드웨어 기능입니다.

디스크에서 대용량 파일을 읽어오거나, 네트워크 카드가 초당 수 기가바이트의 패킷을 처리하는 상황을 생각해보죠. 이 모든 데이터가 매번 CPU 레지스터를 거쳐 메모리로 복사된다면 시스템은 순식간에 병목에 빠질 것입니다. DMA는 바로 이 문제를 해결하기 위해 등장한 개념이며, 오늘날 스토리지, 네트워킹, GPU, 심지어 Thunderbolt나 USB4 같은 외부 포트 보안 논의에서도 핵심적으로 다뤄지는 주제입니다.

## CPU가 모든 걸 처리하던 시절: PIO의 한계

DMA를 이해하려면 먼저 그 이전 방식인 `PIO(Programmed I/O)`를 알아야 합니다. PIO 방식에서는 주변장치와 메모리 사이의 모든 데이터 전송을 CPU가 직접 명령어 단위로 수행합니다. 예를 들어 디스크 컨트롤러에서 1바이트를 읽으려면 CPU가 `IN` 명령어로 값을 레지스터에 읽어들인 뒤, 다시 `MOV` 명령어로 메모리에 써넣는 과정을 데이터 크기만큼 반복해야 합니다.

이 방식은 두 가지 치명적인 문제를 가지고 있습니다.

- **CPU 점유율 폭증**: 대용량 전송 중에는 CPU가 다른 작업을 전혀 처리하지 못하고 단순 복사 작업에 묶여버립니다.
- **속도 병목**: CPU의 명령어 처리 사이클이 곧 전송 속도의 상한선이 되어, 고속 주변장치의 성능을 온전히 활용할 수 없습니다.

이런 한계를 극복하기 위해 시스템 버스에 별도의 전송 담당 하드웨어를 두고, CPU는 "시작해"라는 지시만 내린 뒤 다른 일을 하도록 설계된 것이 DMA입니다.

## DMA의 동작 원리

### DMA 컨트롤러와 버스 마스터링

DMA 전송의 핵심에는 `DMAC(DMA Controller)`라는 전용 하드웨어가 있습니다. DMAC은 CPU를 대신해 주소 버스와 데이터 버스를 직접 제어할 수 있는 `버스 마스터링(Bus Mastering)` 권한을 가집니다. 현대 시스템에서는 별도의 중앙 DMAC 칩보다는 각 주변장치, 특히 NVMe SSD나 네트워크 카드 같은 `PCIe(PCI Express)` 장치가 자체적으로 DMA 엔진을 내장한 형태가 일반적입니다.

전송 절차는 대략 다음과 같습니다.

1. CPU가 DMAC 또는 장치의 레지스터에 전송할 소스 주소, 목적지 주소, 전송 길이를 설정합니다.
2. CPU가 전송 시작 신호를 보내고, 곧바로 다른 명령어 실행으로 복귀합니다.
3. DMAC이 시스템 버스의 제어권을 요청하고, `버스 중재기(Bus Arbiter)`가 이를 승인하면 CPU는 일시적으로 버스 사용을 양보합니다.
4. DMAC이 소스에서 목적지로 데이터를 직접 옮깁니다. 이 구간에서 CPU는 관여하지 않습니다.
5. 전송이 완료되면 DMAC이 `인터럽트(Interrupt)`를 발생시켜 CPU에게 작업이 끝났음을 알립니다.

<img width="2720" height="1280" alt="dma_architecture_diagram" src="https://github.com/user-attachments/assets/b1aa9d9c-d957-42ec-95e7-11c558fe16ce" />

### 전송 모드

DMA는 버스를 얼마나 오래 점유하느냐에 따라 몇 가지 방식으로 나뉩니다.

- **버스트 모드(Burst Mode)**: 전체 데이터를 한 번에 연속으로 전송합니다. 속도는 가장 빠르지만, 전송이 끝날 때까지 CPU가 버스를 전혀 사용할 수 없습니다.
- **사이클 스틸링(Cycle Stealing)**: 한 번에 한 워드씩만 전송하고 버스를 CPU에 되돌려주기를 반복합니다. CPU 성능 저하는 적지만 전송 속도는 느려집니다.
- **트랜스페어런트 모드(Transparent Mode)**: CPU가 버스를 사용하지 않는 순간에만 전송을 수행합니다. CPU 성능에 거의 영향을 주지 않지만 가장 느립니다.

### Scatter-Gather DMA

실제 운영체제 메모리는 물리적으로 연속되어 있지 않은 경우가 많습니다. 이를 위해 등장한 것이 `SGDMA(Scatter-Gather DMA)`입니다. SGDMA는 흩어져 있는 여러 메모리 블록의 주소 목록을 담은 `디스크립터 리스트(Descriptor List)`를 미리 구성해두고, DMA 엔진이 이 리스트를 순차적으로 읽으며 여러 조각의 데이터를 하나의 논리적 전송처럼 처리합니다. NVMe 프로토콜의 `PRP(Physical Region Page)`나 `SGL(Scatter Gather List)` 구조가 바로 이 개념을 구현한 대표적인 사례입니다.

## 보안 관점에서 본 DMA: 양날의 검

DMA의 가장 큰 특징이자 위험 요소는, 주변장치가 운영체제의 메모리 보호 메커니즘을 우회하여 물리 메모리에 직접 접근한다는 점입니다. CPU 기반의 접근 제어나 커널의 페이지 권한 검사를 거치지 않기 때문에, 악의적으로 조작된 주변장치는 이론적으로 메모리의 모든 영역을 읽거나 쓸 수 있습니다. 이러한 공격 기법을 흔히 `DMA 공격(DMA Attack)`이라 부릅니다.

### 대표적인 공격 사례

- **FireWire 공격**: 초기 FireWire 포트는 별도의 인증 절차 없이 DMA 접근을 허용해, 잠금 화면을 우회하고 메모리에서 암호를 추출하는 공격이 다수 보고되었습니다.
- **PCILeech / Thunderclap**: PCIe 확장 카드나 Thunderbolt 장치를 이용해 커널 메모리를 덤프하거나 임의 코드를 주입하는 연구가 발표되며, 노트북의 Thunderbolt 포트 역시 잠재적 공격 표면임이 드러났습니다. [Thunderclap 관련 블로그](https://blog.alyac.co.kr/2163)
- **악성 주변장치를 통한 권한 상승**: 물리적 접근이 가능한 공격자가 조작된 USB4/Thunderbolt 동글을 꽂는 것만으로 시스템을 장악하는 시나리오가 다수의 침투 테스트에서 검증되었습니다.

### 방어 기술: IOMMU

이런 위협에 대응하기 위해 등장한 것이 `IOMMU(Input-Output Memory Management Unit)`입니다. IOMMU는 CPU의 `MMU(Memory Management Unit)`가 프로세스별 가상 메모리를 격리하듯, 각 주변장치가 접근할 수 있는 물리 메모리 범위를 별도의 주소 변환 테이블로 제한합니다. 인텔 플랫폼의 `VT-d(Virtualization Technology for Directed I/O)`와 AMD 플랫폼의 `AMD-Vi`가 이를 구현한 대표적인 기술입니다.

IOMMU가 활성화된 환경에서는 장치가 사전에 커널로부터 매핑받은 주소 영역 밖으로는 DMA 요청을 보낼 수 없어, 임의 메모리 접근이 원천적으로 차단됩니다. Windows에서는 다음과 같은 명령으로 IOMMU 활성화 여부와 관련 PCI 장치 상태를 확인할 수 있습니다.

```powershell
# IOMMU가 커널에 의해 인식되었는지 확인
Get-PnpDevice -PresentOnly | Where-Object { $_.InstanceId -match "^PCI" } |
  Select-Object FriendlyName, InstanceId, Status

# 커널 DMA 보호(Kernel DMA Protection) 활성화 여부 확인
msinfo32
```

`msinfo32`를 실행한 뒤 시스템 요약 화면에서 커널 DMA 보호 항목이 켬으로 표시되는지 확인하면 됩니다. 이 값이 꺼져 있다면 해당 하드웨어가 IOMMU 기반의 DMA 격리를 지원하지 않거나 BIOS/UEFI에서 VT-d 혹은 AMD-Vi가 비활성화되어 있을 가능성이 큽니다.

<img width="1940" height="834" alt="image" src="https://github.com/user-attachments/assets/1340ad7b-8f7c-49fc-920f-4eabc415b64e" />


Thunderbolt 장치의 경우, 최신 운영체제는 `Kernel DMA Protection` 기능을 통해 사용자가 명시적으로 신뢰하지 않은 장치의 DMA 요청을 부팅 초기부터 차단하도록 기본 정책을 강화하고 있습니다. Windows에서는 `설정 > 개인 정보 및 보안 > Windows 보안 > 디바이스 보안 > 코어 격리` 또는 시스템 정보 앱의 `커널 DMA 보호` 항목으로, macOS에서는 시스템 정보의 `Thunderbolt/USB4` 보안 설정으로 이 상태를 확인할 수 있습니다.

## 실무 활용: DMA 성능과 상태 확인

DMA는 보안뿐 아니라 성능 튜닝에서도 자주 등장합니다. Windows 환경에서 NVMe 장치나 네트워크 카드가 DMA를 정상적으로 활용하고 있는지는 다음과 같이 점검할 수 있습니다.

```powershell
# 디스크의 버스 유형 확인 (NVMe/AHCI는 기본적으로 DMA·버스 마스터링을 사용)
Get-PhysicalDisk | Select-Object FriendlyName, MediaType, BusType

# 레거시 IDE/ATA 컨트롤러의 전송 모드 확인
Get-CimInstance -ClassName Win32_IDEController | Select-Object Name, Status

# 디스크 처리량 실시간 모니터링 (병목 여부 확인)
Get-Counter '\PhysicalDisk(*)\Disk Bytes/sec'

# 네트워크 어댑터의 전송 통계 및 드롭 확인
Get-NetAdapterStatistics -Name "이더넷" |
  Select-Object OutboundDiscardedPackets, InboundDiscardedPackets
```

레거시 IDE 계열 장치의 DMA 모드는 `장치 관리자 > IDE ATA/ATAPI 컨트롤러 > 채널 속성 > 고급 설정` 탭에서 `현재 전송 모드(Current Transfer Mode)` 값으로도 직접 확인할 수 있습니다. NVMe나 SATA AHCI처럼 최신 인터페이스는 별도 설정 없이 항상 DMA·버스 마스터링을 사용하므로, 이 경우에는 `리소스 모니터(resmon.exe)`의 CPU 탭에서 인터럽트와 DPC(지연 프로시저 호출) 사용량을 확인해 인터럽트 폭주로 인한 성능 저하가 있는지 살펴보는 편이 더 실질적입니다.

이런 지표를 통해 DMA 전송이 병목 없이 이루어지고 있는지, 혹은 인터럽트 폭주로 인해 예상만큼의 성능이 나오지 않는지를 진단할 수 있습니다.

## 게임 보안 관점에서 본 DMA: DMA 치트

DMA의 물리 메모리 직접 접근 능력은 최근 몇 년간 온라인 게임의 부정행위 진영에서도 적극적으로 악용되고 있습니다. 흔히 `DMA 치트(DMA Cheat)`라 불리는 이 방식은, 대상 PC에 PCIe 슬롯으로 꽂는 별도의 FPGA 기반 캡처 카드를 이용해 게임이 실행 중인 컴퓨터의 물리 메모리를 외부에서 직접 읽어오는 방식으로 동작합니다.

### 왜 게임 핵이 DMA를 사용하게 되었는가

기존의 소프트웨어 기반 치트는 메모리 스캐너, DLL 인젝션, API 후킹처럼 대상 게임과 같은 OS, 같은 CPU 위에서 실행될 수밖에 없었습니다. BattlEye나 EAC(Easy Anti-Cheat), Vanguard처럼 커널 모드로 동작하는 안티치트는 바로 이 지점을 노려, 실행 중인 프로세스 목록, 로드된 드라이버의 서명, 후킹된 함수, 메모리 무결성을 지속적으로 검사합니다. 즉 치트가 대상 PC 안에서 돌아가는 한, 이론적으로는 언젠가 탐지될 여지가 남습니다.

DMA 치트는 이 전제 자체를 깨버립니다. 치트 로직을 대상 PC의 CPU나 OS 위에서 아예 실행하지 않고, 물리적으로 분리된 캡처 카드와 보조 PC로 옮겨버리는 것입니다. 대상 PC 입장에서 이 카드는 그저 또 하나의 PCIe 주변장치일 뿐이므로, 안티치트가 아무리 프로세스와 드라이버를 샅샅이 뒤져도 애초에 검사할 대상 자체가 존재하지 않습니다.

- **완전한 외부 실행**: 치트가 대상 게임의 프로세스 공간이나 커널 드라이버 목록에 전혀 나타나지 않습니다. 안티치트가 스캔할 수 있는 것은 파일, 프로세스, 메모리 서명인데, DMA 치트는 이 중 어느 것도 대상 PC에 남기지 않습니다.
- **가상 메모리 보호 우회**: 소프트웨어 치트는 OS가 관리하는 가상 주소 공간과 접근 권한 검사를 거쳐야 하지만, DMA는 물리 주소에 직접 접근하므로 이런 커널 레벨의 권한 검사 자체를 통과할 필요가 없습니다.
- **읽기와 쓰기 모두 가능**: 캐릭터 좌표나 체력 같은 값을 실시간으로 읽어 화면에 표시하는 것(월핵, ESP)뿐 아니라, 특정 조건에서는 메모리 값을 직접 조작하는 것도 물리적으로는 가능합니다.
- **낮은 지연시간**: 고속 PCIe 버스를 통해 데이터를 가져오기 때문에, 별도 프로세스에서 폴링하는 소프트웨어 방식보다 지연시간이 짧아 실시간성이 중요한 에임봇류 치트에도 활용됩니다.

<img width="2720" height="1680" alt="dma_cheat_architecture_diagram" src="https://github.com/user-attachments/assets/34e44ff6-40e2-416f-87fb-b11171dfe536" />


### 게임사는 DMA 치트에 어떻게 대응해야 하는가

DMA 치트는 대상 PC의 소프트웨어 영역을 벗어나 있기 때문에, 기존의 프로세스·드라이버 스캔 방식만으로는 근본적인 대응이 어렵습니다. 따라서 방어도 하드웨어 레벨과 행동 기반 탐지를 함께 고려해야 합니다.

- **하드웨어 존재 자체를 탐지**: 알려진 DMA 캡처 카드의 PCI 벤더 ID·디바이스 ID, 클래스 코드, IOMMU 그룹 구성의 이상 여부를 커널 모드 안티치트에서 주기적으로 점검해, 의심스러운 PCIe 장치가 새로 연결되었는지 확인합니다.
- **커널 DMA 보호 활성화 유도**: 사용자의 시스템에서 IOMMU, 즉 인텔 VT-d나 AMD AMD-Vi, 그리고 Windows의 `Kernel DMA Protection`이 켜져 있는지 점검하고, 꺼져 있다면 활성화를 권장하거나 게임 실행 조건으로 요구하는 방식을 검토할 수 있습니다. 신뢰되지 않은 장치의 DMA 요청 자체를 차단하면 치트 카드가 애초에 물리 메모리에 접근할 수 없습니다.
- **메모리 상의 데이터 난독화**: 좌표, 체력, 탄약 수 같은 민감한 값을 물리 메모리에도 평문으로 두지 않고 암호화하거나, 포인터 체인과 구조체 레이아웃을 실행마다 무작위화해 DMA로 덤프한 메모리에서 의미 있는 값을 추출하기 어렵게 만듭니다.
- **하이퍼바이저 기반 안티치트**: 안티치트 로직 자체를 `VBS(Virtualization-Based Security)`나 하이퍼바이저 레벨로 옮겨, 게스트 OS의 가상 메모리와 실제 물리 메모리 사이의 매핑을 게임사가 통제하는 계층에서 관리하면 DMA 장치의 비정상적인 물리 메모리 접근 패턴을 탐지하기 수월해집니다.
- **서버 사이드 행동 기반 탐지**: 하드웨어 자체를 완전히 막기 어렵다는 전제하에, 반응 속도, 조준 정확도, 시야각 변화 패턴처럼 사람이 물리적으로 재현하기 힘든 통계적 이상치를 서버에서 분석해 하드웨어 치트를 우회 탐지하는 것도 중요한 보완책입니다.

결국 DMA 치트에 대한 방어는 단일 해법이 아니라, 하드웨어 신뢰 경계 관리와 데이터 난독화, 행동 기반 탐지를 겹겹이 쌓는 심층 방어 접근이 현실적인 대응 방향입니다.

## 정리: 성능과 보안 사이의 균형

DMA는 CPU를 반복적인 데이터 복사 작업에서 해방시켜 시스템 전체의 처리량을 극적으로 끌어올린 핵심 기술입니다. 스토리지, 네트워크, GPU를 비롯한 거의 모든 고속 주변장치가 DMA 없이는 제 성능을 낼 수 없다고 해도 과언이 아닙니다.

동시에 DMA는 CPU 기반 메모리 보호 체계를 우회한다는 근본적인 특성 때문에 오랫동안 물리적 공격 표면으로 지목되어 왔습니다. 초기에는 이 위험이 방치되다시피 했지만, IOMMU와 Kernel DMA Protection 같은 기술이 표준화되면서 신뢰할 수 없는 장치의 DMA 요청을 세밀하게 통제할 수 있게 되었습니다.

결국 DMA를 안전하게 활용한다는 것은 단순히 기능을 켜고 끄는 문제가 아니라, **어떤 장치에게 얼마나 넓은 메모리 접근 권한을 허용할 것인가**라는 최소 권한 원칙을 하드웨어 수준까지 확장하는 문제라고 할 수 있습니다. 노트북 사용자라면 신뢰하지 않는 Thunderbolt나 USB4 장치를 함부로 연결하지 않는 습관만으로도, 그리고 서버 운영자라면 IOMMU 격리 설정을 점검하는 것만으로도 이 오래된 기술이 남긴 공격 표면을 상당 부분 줄일 수 있습니다.
