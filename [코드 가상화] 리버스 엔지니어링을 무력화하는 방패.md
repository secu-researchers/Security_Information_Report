# 코드 가상화: 리버스 엔지니어링을 무력화하는 방패

> 작성자: 김시훈(Kim Sihoon)

-----

코드 가상화(Code Virtualization)란, 원본 프로그램의 명령어를 CPU가 직접 실행할 수 없는 독자적인 바이트코드로 변환한 뒤, 이를 해석하는 전용 인터프리터(가상 머신)를 통해서만 실행되도록 만드는 소프트웨어 보호 기법입니다.

<img height="400" alt="image" src="https://github.com/user-attachments/assets/0ded0b37-0fd7-4ed2-a69e-6ebef6ee4d14" />
<br/><br/>

이름 때문에 VMware나 Hyper-V 같은 하드웨어 가상화를 떠올리기 쉽지만, 완전히 다른 개념입니다. 코드 가상화는 물리적인 서버나 OS를 가상화하는 것이 아니라, 코드 실행 방식 자체를 가상화합니다. 상용 솔루션으로는 VMProtect, Themida, Code Virtualizer 등이 대표적이며, 상용 소프트웨어의 라이선스 체크 로직 보호부터 게임의 안티치트 시스템, 그리고 안타깝게도 악성코드의 분석 회피에 이르기까지 폭넓게 활용됩니다.

## 왜 필요한가: 기존 난독화의 한계

코드 가상화를 이해하려면 먼저 기존 보호 기법들이 왜 뚫리는지 알아야 합니다.

일반적인 네이티브 바이너리는 x86이나 ARM 같은 표준 명령어 집합(ISA, Instruction Set Architecture)으로 컴파일됩니다. 이 명령어 집합은 공개된 표준이기 때문에, IDA Pro나 Ghidra 같은 디스어셈블러는 바이너리를 열자마자 사람이 읽을 수 있는 어셈블리 코드로 즉시 변환해 보여줍니다. 여기에 문자열 암호화나 컨트롤 플로우 평탄화(Control Flow Flattening) 같은 난독화를 추가해도, 결국 CPU가 실행하는 명령어 자체는 x86 표준을 벗어나지 않기 때문에 숙련된 분석가는 시간을 들이면 로직을 복원할 수 있습니다.

코드 가상화는 이 전제 자체를 깨뜨립니다. 표준 명령어 집합 대신, 개발자가 임의로 설계한 나만의 명령어 집합을 사용하는 것입니다. 이 독자 명령어 집합에는 공개된 스펙도, 참고할 디스어셈블러도 존재하지 않습니다. 분석가는 이제 어셈블리를 읽는 것이 아니라, 처음 보는 바이트코드 언어의 문법 자체를 처음부터 역설계해야 합니다.

## 동작 원리: VM-Entry에서 VM-Exit까지

코드 가상화가 적용된 프로그램의 실행 흐름은 크게 세 요소로 구성됩니다.

> **핵심 3요소**
> 1. **바이트코드(Bytecode):** 원본 명령어를 변환한 독자 형식의 데이터
> 2. **가상 CPU 컨텍스트(Virtual CPU Context):** 실제 레지스터를 대신하는 가상의 레지스터와 스택 공간
> 3. **인터프리터(Interpreter):** 바이트코드를 한 줄씩 읽어 해석하고 실제 동작을 수행하는 핸들러 루프

<br/>
<img height="700" alt="image" src="https://github.com/user-attachments/assets/427fda04-c3e1-4485-91be-845726d6d37e" />
<br/><br/>

보호할 함수의 원본 코드는 컴파일 시점 또는 별도의 변환 도구를 통해 바이트코드로 치환됩니다. 예를 들어 `mov eax, ebx`라는 단순한 명령어가 `0x4A 0x02 0x1F` 같은 임의의 바이트 시퀀스로 바뀌는 식입니다. 이 매핑 규칙은 오직 해당 프로그램의 인터프리터만 알고 있습니다.

<img height="400" alt="image" src="https://github.com/user-attachments/assets/219324b6-bd2e-45dd-90a0-dab5400a3848" />
<br/><br/>

실제 실행 시점에는 다음과 같은 사이클이 반복됩니다.

1. **VM-Entry:** 실제 CPU의 레지스터 값들이 가상 CPU 컨텍스트, 즉 메모리상의 특정 구조체로 저장됩니다.
2. **Fetch:** 인터프리터가 바이트코드 스트림에서 명령어 하나를 읽어옵니다.
3. **Dispatch:** 읽어온 바이트코드 값에 해당하는 핸들러 함수로 분기합니다. 이 구조는 대개 거대한 `switch-case`문이나 점프 테이블(Jump Table) 형태로 구현됩니다.
4. **Execute:** 해당 핸들러가 가상 레지스터나 가상 스택에 대해 원본 명령어와 동일한 연산을 수행합니다.
5. **Loop:** 다음 바이트코드로 이동해 2번 과정을 반복합니다.
6. **VM-Exit:** 보호 구간이 끝나면 가상 CPU 컨텍스트의 값을 다시 실제 CPU 레지스터로 복원하고, 이후 네이티브 코드 실행을 이어갑니다.

이 구조의 핵심은 실제 CPU가 바이트코드를 직접 이해하지 못한다는 점입니다. CPU는 오직 인터프리터라는 하나의 하나의 인터프리터 루프만 반복 호출할 뿐이며, 프로그램의 진짜 로직은 데이터 형태로 존재하는 바이트코드 속에 숨겨져 있습니다.

## 분석을 어렵게 만드는 구체적 요인

정적 분석 관점에서, 디스어셈블러는 인터프리터의 `switch-case` 분기 루프만 보게 될 뿐 실제 프로그램 로직은 확인할 수 없습니다. 상용 솔루션들은 여기에 더해 여러 겹의 방어 장치를 추가합니다.

- **핸들러 다형성(Handler Polymorphism):** 동일 연산을 여러 변종 형태로 생성해 두어 패턴 기반 시그니처 탐지를 무력화합니다.
- **명령어 집합 재배치(opcode/bytecode randomization):** 바이너리마다, 혹은 컴파일할 때마다 바이트코드 매핑 규칙 자체가 달라져, 한 번 분석한 결과를 다른 샘플에 재사용할 수 없습니다.
- **중첩 가상화(Nested Virtualization):** VM 인터프리터 자체를 또 다른 VM으로 한 번 더 감싸, 분석가가 인터프리터 로직을 파악하는 단계에서부터 좌절하게 만듭니다.
- **안티 디버깅 통합:** 디버거 탐지 루틴을 VM 핸들러 내부에 삽입해, VM 바깥에서의 일반적인 안티 디버깅 우회 기법이 통하지 않도록 합니다.

동적 분석 관점에서는 또 다른 어려움이 있습니다. 디버거로 스텝 실행을 하더라도, 분석가는 원본 명령어가 아니라 인터프리터의 fetch-dispatch-execute 루프만 수백, 수천 번 반복해서 보게 됩니다. 단 하나의 원본 `add` 연산을 이해하기 위해 실제로는 수십 개의 인터프리터 명령어를 따라가야 하는 경우도 흔하며, 이로 인해 실행 속도 자체도 네이티브 코드 대비 수백~수천 배까지 느려지는 트레이드오프가 발생합니다.

### x64DBG를 이용한 가상화된 함수 동작 방식

x64dbg를 켜고 가상화된 함수를 실행시키면:

1. 레지스터 창(RAX, RSP 등)에는 별다른 규칙성이 안 보입니다. 왜냐하면 진짜 연산은 인터프리터 루프(핸들러들)가 반복 실행되면서 이루어지고, 그 과정에서 진짜 레지스터는 "바이트코드를 읽고, 가상 컨텍스트 메모리 주소를 계산하는" 작업에 계속 재사용되기 때문입니다.
2. 가상 CPU 컨텍스트가 위치한 메모리 주소를 찾아서 덤프로 열어보면, 그 안에서 값들이 원본 코드의 흐름대로 변경되는 걸 관찰할 수 있습니다. 이것이 사실상 "가상 레지스터"의 실체입니다.
3. RIP는 인터프리터 루프 안(핸들러들 사이)을 계속 반복적으로 접근합니다. 원본 함수의 주소 범위를 벗어나지 않는 일반 코드와 달리, RIP가 `디스패처 → 핸들러 → 디스패처 → 다른 핸들러...` 이런 식으로 좁은 범위를 맴돕니다. 이것이 "이 함수가 가상화됐다"를 알아채는 대표적인 단서 중 하나입니다.

## 실무에서의 관찰 지점: 역가상화 접근법

방어 기법이 강력한 만큼, 이를 분석하기 위한 역가상화(Devirtualization) 연구도 활발합니다. 실무에서 주로 사용되는 접근은 다음과 같습니다.

```
# Taint Analysis 개념 예시 (의사코드)
# VM-Entry 시점의 레지스터를 오염원(Taint Source)으로 지정
mark_tainted(context.eax_slot)

# 이후 바이트코드 실행을 트레이싱하며
# 오염된 데이터가 어떤 핸들러를 거쳐 전파되는지 기록
for instruction in bytecode_trace:
    if instruction.reads_tainted_slot():
        log_handler_semantics(instruction.handler_addr)
        propagate_taint(instruction)
```

**심볼릭 실행(Symbolic Execution)** 과 **테인트 분석(Taint Analysis)** 을 결합해 VM-Entry부터 VM-Exit까지의 전체 실행 트레이스를 기록한 뒤, 실제 값 대신 기호(Symbol)를 흘려보내며 각 핸들러가 수행하는 연산의 의미를 역으로 추론하는 방식이 널리 쓰입니다. 이 과정을 자동화한 도구로는 Triton, Miasm 등이 있으며, 충분한 트레이스가 누적되면 핸들러별 시맨틱을 하나씩 복원해 최종적으로는 바이트코드를 다시 원본에 가까운 의사코드로 재구성할 수 있습니다.

다만 이러한 역가상화는 상당한 시간과 전문성을 요구하며, 앞서 언급한 중첩 가상화나 다형성 핸들러가 적용된 경우 자동화 도구의 정확도가 크게 떨어지는 한계가 있습니다.

## 한계와 실무적 시사점

코드 가상화는 만능이 아닙니다. 몇 가지 뚜렷한 한계가 존재합니다.

첫째, **성능 저하**입니다. 인터프리터 루프를 거치는 구조상 네이티브 실행 대비 심각한 속도 저하가 발생하므로, 프로그램 전체가 아니라 라이선스 검증이나 핵심 알고리즘 같은 민감한 일부 함수에만 선택적으로 적용하는 것이 일반적입니다.

둘째, **바이너리 크기 증가**입니다. 인터프리터 코드와 핸들러 테이블이 추가되면서 파일 크기가 눈에 띄게 커집니다.

셋째, 코드 가상화는 취약점 자체를 제거하는 기술이 아니라 **분석에 걸리는 시간과 비용을 늘리는 지연 전략**에 가깝습니다. 앞서 살펴본 역가상화 기법들이 보여주듯, 충분한 자원과 시간이 투입되면 결국 분석은 가능합니다. 따라서 이를 방어 전략에 포함할 때는 코드 가상화를 유일한 방어선으로 삼기보다, 서버 측 검증·코드 서명·안티 디버깅·무결성 체크 등과 결합한 **심층 방어(Defense in Depth)** 전략의 한 계층으로 활용하는 것이 바람직합니다.

또한 이 기술이 정당한 소프트웨어 보호뿐 아니라 악성코드의 백신 우회 목적으로도 널리 악용된다는 점에서, 보안 분석가 입장에서는 VM 기반 패커나 프로텍터를 탐지하는 것 자체가 하나의 중요한 트리아지(Triage) 단계로 다뤄집니다. 실제로 엔트로피 분석이나 알려진 VM 시그니처 매칭을 통해 특정 샘플이 코드 가상화로 보호되었는지 여부를 사전에 판별하는 것이, 이후 정밀 분석에 투입할 자원을 결정하는 첫 단추가 됩니다.
