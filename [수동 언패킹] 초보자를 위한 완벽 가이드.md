# 수동 언패킹: 초보자를 위한 완벽 가이드

> 작성자: 김시훈(Kim Sihoon)

-----

**수동 언패킹(Manual Unpacking)**이란, 자동화 도구 없이 디버거와 메모리 분석 기법을 직접 활용하여 압축·암호화된 실행 파일에서 원본 코드를 복원해내는 리버스 엔지니어링 작업을 의미합니다.

## 1. 언패킹이 필요한 이유: 패커의 존재 목적

악성코드 제작자는 백신 탐지를 회피하고 분석을 지연시키기 위해 `패커(Packer)`를 사용합니다. 패커는 원본 바이너리를 압축하거나 암호화한 뒤, 실행 시점에 이를 메모리 상에서 원상태로 복원하는 `스텁(Stub)` 코드를 앞단에 붙입니다.

대표적으로 `UPX`처럼 단순 압축만 수행하는 패커도 있지만, `VMProtect`나 `Themida`처럼 코드 자체를 가상 머신(VM) 명령어로 치환하여 분석 난이도를 극단적으로 높이는 상용 프로텍터도 존재합니다. 정적 분석 도구는 이런 패커가 감싸고 있는 실제 로직을 읽어낼 수 없기 때문에, 분석가는 **프로그램을 직접 실행시켜 메모리에 원본 코드가 펼쳐지는 순간을 포착**해야 합니다.

## 2. 배경지식: PE 구조와 OEP

수동 언패킹을 이해하려면 먼저 Windows 실행 파일 포맷인 `PE(Portable Executable)` 구조를 알아야 합니다.

- **Entry Point**: 프로그램 실행이 시작되는 메모리 주소. 패킹된 파일에서는 이 주소가 원본 코드가 아닌 압축 해제 스텁(Stub)을 가리킵니다.
- **OEP(Original Entry Point)**: 패킹되기 이전, 원본 프로그램이 실제로 실행을 시작하던 진짜 진입점. 수동 언패킹의 최종 목표는 바로 이 OEP를 찾아내는 것입니다.
- **IAT(Import Address Table)**: 프로그램이 호출하는 Windows API 함수들의 실제 주소가 기록된 테이블. 패커는 분석 방해를 위해 이 테이블을 암호화하거나 런타임에 동적으로 채워 넣는 경우가 많아, 언패킹 후 반드시 복구해야 하는 핵심 요소입니다.

```
[이미지 설명: 패킹된 PE 파일의 실행 흐름도. Entry Point에서 시작한 스텁 코드가
메모리 상에 원본 코드를 압축 해제(unpack)하여 특정 메모리 영역에 쓰고,
마지막에 JMP 명령어로 OEP로 점프하는 과정을 화살표로 표현한 다이어그램]
```

## 3. 동작 원리: 어떻게 원본을 복원하는가

패킹된 프로그램의 실행 흐름은 대략 다음과 같습니다.

1. 패커 스텁이 실행되며 압축·암호화된 원본 코드 블록을 메모리에 로드
2. 스텁 내부의 압축 해제 루틴이 원본 바이트를 하나씩 복원하여 별도 메모리 섹션에 기록
3. 복원이 끝나면 IAT를 재구성(API 주소를 다시 채워 넣음)
4. 마지막으로 `JMP` 또는 `PUSH + RET` 명령어를 통해 OEP로 실행 흐름을 넘김

분석가가 노리는 지점이 바로 3번과 4번 사이입니다. 이 시점에는 메모리 상에 이미 완전히 복원된 원본 코드가 존재하기 때문에, 여기서 프로세스 메모리를 통째로 덤프하면 언패킹된 실행 파일을 얻을 수 있습니다.

### 3.1 OEP를 찾는 대표 기법

**RSP 트릭(RSP Trick)**은 64비트 환경에서 가장 널리 쓰이는 수동 기법입니다. x64 아키텍처에서는 32비트에 있던 `PUSHAD`/`POPAD` 명령어가 제거되었기 때문에, 패커 스텁은 각 범용 레지스터(RAX, RBX, RCX, RDX, RSI, RDI, RBP, R8~R15)를 개별 `PUSH`/`POP` 명령어로 하나씩 저장하고 복원하는 방식을 사용합니다.

기법 자체는 다음과 같이 진행됩니다. 

1. 프로그램이 로드되어 스텁의 최초 명령어가 실행되는 시점, 즉 RSP 레지스터가 아직 원본 상태를 가리키고 있는 그 순간의 스택 주소를 확인합니다. 
2. 이 주소에 하드웨어 액세스 브레이크포인트를 걸어두면, 스텁이 저장해두었던 레지스터 값들을 `POP` 명령어로 하나씩 되돌리는 과정에서 스택 포인터가 다시 그 주소를 참조하게 되고, 바로 이 지점에서 실행이 정지합니다. 
3. 이 시점 직후 몇 줄 안에 OEP로의 `JMP`가 위치하는 경우가 대부분입니다.

이 외에도 메모리 접근 브레이크포인트를 활용해 새로 쓰여지는 코드 섹션을 감시하는 방법, 특정 API의 마지막 호출 지점을 추적하는 방법 등이 실무에서 함께 사용됩니다.

## 4. 실무 워크플로우와 도구 활용

실제 분석 현장에서는 다음과 같은 순서로 작업이 진행됩니다.

### 4.1 사전 정찰: 패커 식별

무작정 디버거부터 켜지 않습니다. 먼저 `PEiD`나 `DIE(Detect It Easy)` 같은 시그니처 기반 도구로 어떤 패커가 사용되었는지 확인합니다.

```
$ die.exe sample.exe
PE64
    Packer: UPX(3.96)[NRV,best]
```

알려진 패커라면 `UPX -d`처럼 공개된 언패킹 툴로 즉시 해결되는 경우도 많아, 굳이 수동 분석에 들어갈 필요가 없는지부터 판단하는 것이 시간 절약의 핵심입니다.

시그니처 매칭은 이미 알려진 패커에만 통합니다. 커스텀 패커나 시그니처가 등록되지 않은 변종을 걸러내려면 `엔트로피(Entropy)` 분석을 함께 활용해야 합니다. 엔트로피는 특정 데이터 블록이 얼마나 무작위에 가까운지를 0~8 사이 값으로 나타내는 섀넌 엔트로피(Shannon Entropy) 수치로, 일반적인 코드나 문자열 섹션은 4~6 사이에 머무는 반면 압축되거나 암호화된 데이터는 7.2 이상, 특히 8에 근접한 값을 보입니다. `DIE`와 `PEStudio`는 섹션별 엔트로피를 자동으로 계산해 보여주는데, 예를 들어 `.text` 섹션임에도 엔트로피가 7.5를 넘는다면 시그니처로 잡히지 않는 패커나 프로텍터가 적용되었을 가능성이 높다는 신호로 해석할 수 있습니다.

```
$ pestudio.exe sample.exe
Section    Entropy
.text      7.89   <- 비정상적으로 높음, 패킹/암호화 의심
.rdata     3.42
.data      2.10
```

### 4.2 디버거 진입: x64dbg

상용 패커나 커스텀 패커라면 디버거를 직접 붙여야 합니다. 요즘 유포되는 샘플 대부분이 64비트 PE인 만큼, 실무에서는 `x64dbg`의 64비트 모듈이 사실상 표준 도구로 자리 잡았습니다.

- 파일을 `x64dbg(x64)`로 로드한 뒤, `Options → Just-In-Time` 등 안티 디버깅 우회 설정을 먼저 점검합니다.
- 모듈 진입 직후, 즉 스텁의 최초 명령어가 실행되는 시점의 `RSP` 레지스터 값을 레지스터 창에서 확인합니다.
- 해당 스택 주소에 하드웨어 브레이크포인트(`Follow in Dump → Breakpoint → Hardware, Access`)를 설정합니다. x64dbg는 64비트 주소를 그대로 지원하므로 별도 변환 없이 값을 그대로 입력하면 됩니다.
- 다시 실행(`F9`)하면 스텁이 저장해둔 레지스터들을 `POP` 명령어로 하나씩 복원하며 RSP가 원래 값으로 되돌아오는 순간 브레이크포인트가 걸리고, 그 직후가 곧 OEP 부근입니다.

```
00007FF6`4A3C10A0 | 41 5F         | pop r15
00007FF6`4A3C10A2 | 41 5E         | pop r14
00007FF6`4A3C10A4 | 5F            | pop rdi
00007FF6`4A3C10A5 | 5E            | pop rsi
00007FF6`4A3C10A6 | 5D            | pop rbp
00007FF6`4A3C10A7 | E9 5A1F0000   | jmp sample.7FF64A3C2F06   ; <- 이 주소가 OEP
```

### 4.3 메모리 덤프: Scylla / OllyDumpEx

OEP를 찾았다면 그 시점의 프로세스 메모리를 파일로 떠내야 합니다. `Scylla`는 x64dbg 플러그인으로 통합되어 있어 실무에서 가장 많이 쓰이는 덤프 도구입니다. (일반 프로그램으로도 있습니다!)

- Scylla를 실행해 현재 OEP 값을 입력
- **IAT Autosearch**로 흩어진 API 주소들을 자동 스캔
- **Get Imports**로 임포트 함수 목록을 재구성
- 필요시 `Cut Thunks`로 유효하지 않은 참조를 정리
- **Dump** 버튼으로 현재 메모리 상태를 파일로 저장
- **Fix Dump**로 방금 덤프한 파일에 재구성된 IAT를 연결

이 과정에서 IAT 복구가 제대로 되지 않으면 덤프한 파일은 실행 시 즉시 크래시가 발생하므로, `ImpREC(Import REConstructor)` 같은 대체 도구로 교차 검증하는 것이 실무에서는 일반적입니다.

### 4.4 검증: IDA Pro / Ghidra

최종적으로 덤프된 파일을 `IDA Pro`나 무료 대안인 `Ghidra`로 열어 함수 시그니처, 문자열, 제어 흐름 그래프가 정상적으로 복원되었는지 확인합니다. 정상적인 PE라면 이 단계에서 문자열 참조나 API 호출 트리가 명확하게 드러나야 하며, 여전히 난독화된 구간이 보인다면 다중 계층 패킹을 의심해야 합니다.

## 5. 실무에서 마주치는 한계와 어려움

- **안티 디버깅**: `IsDebuggerPresent`, `NtQueryInformationProcess`, 타이밍 체크 등으로 디버거 부착 자체를 탐지하고 실행을 중단시키는 경우가 흔합니다. `ScyllaHide` 같은 플러그인으로 이런 탐지를 우회해야 작업이 가능합니다.
- **다중 레이어 패킹**: OEP에 도달했다고 끝이 아니라, 그 코드가 또 다른 패커의 스텁인 경우가 있어 위 과정을 여러 번 반복해야 할 수 있습니다.
- **VM 기반 프로텍터**: `VMProtect`처럼 x64 명령어를 자체 바이트코드로 치환하는 경우, RSP 트릭 자체가 무의미해지며 훨씬 정교한 VM 핸들러 분석이 요구됩니다.
- **자가 수정 코드(Self-Modifying Code)**: 실행 중 코드 섹션 자체를 계속 덮어쓰는 방식이라 단일 시점 덤프만으로는 전체 로직을 복원할 수 없는 경우도 있습니다.

## 6. 방어자 관점: 이 기술이 왜 중요한가

수동 언패킹은 공격 기술이 아니라 방어와 분석을 위한 핵심 역량입니다. 악성코드 대응팀(CERT), 백신 시그니처 개발자, 침해사고 대응(DFIR) 인력에게는 필수 스킬로 꼽힙니다. 패킹된 샘플의 원본 로직을 확인하지 못하면 C2(Command and Control) 서버 주소, 페이로드의 실제 악성 행위, 유포 방식 등을 파악할 수 없기 때문입니다.

조직 차원에서는 이런 위협에 대응하기 위해 다음과 같은 전략을 함께 고려해야 합니다.

- 샌드박스 기반 동적 분석과 수동 언패킹을 병행하여 분석 시간을 단축
- YARA 룰을 패커 시그니처와 언패킹된 코드 패턴 양쪽에 모두 작성
- EDR 솔루션에서 메모리 상 코드 실행(사후 언패킹 시점)을 별도로 모니터링

## 7. 요약

수동 언패킹은 레지스터 `PUSH`/`POP` 복원 패턴이나 RSP 트릭 같은 스텁 코드의 공통적인 행동 양식을 이해하고, `x64dbg`로 실행 흐름을 추적해 OEP를 찾은 뒤, `Scylla`로 메모리를 덤프하고 IAT를 복구하는 일련의 과정입니다. 자동화 도구로 해결되지 않는 상용 프로텍터나 커스텀 패커를 마주했을 때, 결국 분석가의 실력을 가르는 것은 이 수작업 과정에 대한 숙련도입니다. 안티 디버깅 우회와 다중 레이어 패킹 대응까지 능숙해지면, 어떤 형태의 패킹된 샘플이 오더라도 원본 코드에 도달할 수 있는 실질적인 분석 역량을 갖추게 됩니다.
